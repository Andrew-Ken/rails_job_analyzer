= form_for @white_list do |f|
  -if @white_list.errors.any?
    #error_explanation
      %h2= "#{pluralize(@white_list.errors.count, "error")} prohibited this white_list from being saved:"
      %ul
        - @white_list.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
